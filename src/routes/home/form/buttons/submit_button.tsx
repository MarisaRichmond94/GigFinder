import { ReactElement } from 'react';

const SubmitButton = (): ReactElement => {
  const onClick = (): void => {

  }

  return (
    <button className='primary-blue-gig-button' onClick={onClick}>
      Find My Dream Gig
    </button>
  )
}

export default SubmitButton;
